## TODO 
* Add decomposition of fields into compressive and left/right handed solenoidal modes. Decomposition should be user-defined and work together with the shell-decomposition. I.e. any combination of decompositions should be supported. Also support outputting decomposed power spectra. 
* ~~Allow decomposing the mediating fields aswell~~ -- done: `DecompositionMode::mediator_resolved` (see `docs/README.md`). Density (rho) is still excluded -- it's only ever used as a `sqrt(rho)` scaling factor (PU, FU, and BUPbb/UBPbb's rho-scaled sides), not a transported field, so shell-restricting it isn't physically meaningful the way it is for U/B/b/div(U)/div(b). Worth revisiting once/if the compressive-solenoidal decomposition above lands, since that TODO explicitly wants "any combination of decompositions" to compose with this one.
* Add transfer function for magnetic helicity
* Add helicity-computation in Lorentz gauge. Output helicity field and spectra. 